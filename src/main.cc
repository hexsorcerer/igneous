#include <iostream>

// just a little something simple while I setup the project
int main(int argc, char** argv) {
  std::cout << "Press q to exit" << std::endl;
  while (getchar() != 'q') { }
  
  return 0;
}
